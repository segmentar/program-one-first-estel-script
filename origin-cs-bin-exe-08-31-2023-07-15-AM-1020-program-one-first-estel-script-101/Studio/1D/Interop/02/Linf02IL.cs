using Core;

namespace Core
{
    using System;

    public partial class Interop
    {
        public static void Linf02IL()
        {
            if (ArchitecturePageOneFirst.HasArgumentLength is false)
            {
                return;
            }
            else
                "false".ToString();

            var roth = (Program.ArgumentArray.Length - 1);

            var eoth = Program.ArgumentArray[roth];

            var lower = eoth.ToLower();

            var safe_boolean__EQUAL_THAN = (lower == "linger");

            Boolean isLingerCheck, shouldReturn;

            isLingerCheck = safe_boolean__EQUAL_THAN is true;

            shouldReturn = isLingerCheck is false;

            if (shouldReturn is true)
            {
                return;
            }
            else
                "false".ToString();

            ArchitecturePageOneFirst.HasLinger = true;

            return;
        }
    }
}
